<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <link href="~/lib/bootstrap/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="~/lib/font-awesome/css/font-awesome.min.css" rel="stylesheet" />
    <link href="~/CSS/styles.css" rel="stylesheet" />

    <title>DutchTreat: @ViewBag.Title</title>
</head>
<body>
    <!--<div class="navbar navbar-inverse">
        <header class="container">
            <div class="navbar-header">
                <div class="navbar-brand"><i class="fa fa-paint-brush"></i> Dutch Treat</div>
                <button class="navbar-toggle " data-toggle="collapse" data-target="#menu">
                    <span class="glyphicon glyphicon-chevron-down"></span>
                </button>
            </div>
            <div id="menu" class="collapse navbar-collapse">
                <ul class="nav navbar-nav">
                    <li><a asp-controller="App" asp-action="Index">Home</a></li>
                    <li><a asp-controller="App" asp-action="Contact">Contact</a></li>
                    <li><a asp-controller="App" asp-action="Shop">Shop</a></li>
                    @if (User.Identity.IsAuthenticated)
                    {
                        <li><a asp-controller="Account" asp-action="Logout">Logout</a></li>
                        <li><a asp-controller="App" asp-action="MyOrder">YourOrders</a></li>
                    }
                    else
                    {
                        <li><a asp-controller="Account" asp-action="Login">Login</a></li>
                    }
                    <li><a asp-controller="App" asp-action="About">About</a></li>
                    <li><input type="text" placeholder="Search.."></li>
                </ul>
            </div>


        </header>
    </div>-->
    <div class="navbar navbar-inverse navbar-static-top" role="navigation">
        <header class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
            </div>

            <div class="collapse navbar-collapse">

                <ul class="nav navbar-nav">
                    <li><a asp-controller="App" asp-action="Index">Home</a></li>
                    <li><a asp-controller="App" asp-action="Contact">Contact</a></li>
                    <li><a asp-controller="App" asp-action="Shop">Shop</a></li>
                    @if (User.Identity.IsAuthenticated)
                    {
                        <li><a asp-controller="Account" asp-action="Logout">Logout</a></li>
                        <li><a asp-controller="App" asp-action="MyOrder">YourOrders</a></li>
                    }
                    else
                    {
                        <li><a asp-controller="Account" asp-action="Login">Login</a></li>
                    }
                    <li><a asp-controller="App" asp-action="About">About</a></li>
                </ul>
                @if (ViewBag.Title == "Shops")
                {
                    @RenderSection("Content", false)
                }
            </div>
        </header>
    </div>

    <section class="container">
        <h2 class="text-center">@ViewBag.Title</h2>
        @RenderBody()
    </section>
    <div class="container">
        <footer class="text-center">
            Copyright &copy; 2017 Dutch treat LLTD
        </footer>
    </div>

    <script src="~/lib/jquery/dist/jquery.js"></script>
    <script src="~/Script/JavaScript.js"></script>
    @RenderSection("scripts", false)
    @RenderSection("stylesheets", false)
</body>
</html>
